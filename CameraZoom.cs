using UnityEngine;

public class CameraZoom : MonoBehaviour
{
    /*public static CameraZoom Instance { get; private set; }

    private Camera camera;

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
        }
        else
        {
            Destroy(gameObject); // Ensures there is only one instance of CameraZoom
        }

        // Ensure camera is assigned
       camera = Camera.main;
    }

    public void CastleZoom()
    {

    }*/

    
}
